# قواعد المشروع العامة — Physics Constants of the Week
### (نسخة معدّلة v2 — تشمل نموذج البيانات وآليات التحقق المفقودة)

> التعديلات على النسخة الأصلية مُشار إليها بـ 🆕 أو ✏️. الأقسام غير المعدَّلة أُبقيت كما هي لأنها سليمة.

---

## 1. الهدف

هذا المشروع مخصص لبناء **قاعدة معرفية علمية موثقة للثوابت الفيزيائية**، مع إنتاج محتوى علمي دوري عنها.

المشروع ليس مولد مقالات فقط، بل نظام يجمع بين:

* البيانات العلمية المنظمة.
* البحث والتحقق العلمي.
* تاريخ الفيزياء.
* المترولوجيا.
* التجارب الفيزيائية.
* المحتوى التعليمي.
* الأتمتة.
* GitHub Actions.
* النشر الإلكتروني.

يجب أن تكون جميع مكونات المشروع قابلة للتوسع وإعادة الاستخدام.

---

## 2. مصدر الحقيقة

**GitHub Repository هو مصدر الحقيقة الأساسي للمشروع (Source of Truth).**

لا يعتبر WordPress أو المقال المنشور مصدر البيانات الأساسي.

البيانات المنظمة هي الأصل، والمقالات والتقارير والمخرجات الأخرى يتم توليدها منها.

العلاقة الأساسية:

```text
Scientific Sources
       ↓
Ingestion Snapshot (🆕 القسم 5-ب)
       ↓
Validated Data
       ↓
Cross-Consistency Check (🆕 القسم 9-ب)
       ↓
GitHub Knowledge Base
       ↓
Generated Content
       ↓
WordPress
```

---

## 3. الدقة العلمية أولًا

الأولوية المطلقة هي:

```text
Scientific Accuracy
>
Source Verification
>
Data Integrity
>
Content Quality
>
SEO
>
Publication Speed
```

لا يجوز التضحية بالدقة العلمية من أجل سرعة الإنتاج، زيادة عدد المقالات، تحسين SEO، زيادة طول المحتوى، أو النشر الأسبوعي.

---

## 4. لا تخمّن

إذا لم تتوفر معلومة موثوقة: **لا تخمّن**، ولا تستخدم المعرفة الداخلية للنموذج كبديل عن المصدر عندما تكون المعلومة قابلة للتحقق.

استخدم حالة `NEEDS_REVIEW` أو `UNVERIFIED` بدل اختلاق قيمة أو معلومة.

---

## 5. المصادر العلمية

رتّب المصادر حسب الموثوقية:

**المستوى الأول:** BIPM · NIST · CODATA · CIPM · CGPM · SI Brochure
**المستوى الثاني:** الأبحاث العلمية المحكمة.
**المستوى الثالث:** الكتب والمراجع الأكاديمية.
**المستوى الرابع:** المصادر التعليمية والثانوية.

لا تستخدم المصادر الثانوية كمصدر أساسي للبيانات العلمية عندما يتوفر المصدر الأصلي أو الرسمي.

### 5-ب. آلية الابتلاع الآلي (Ingestion) 🆕

مصدر الابتلاع الآلي الأساسي هو ملف CODATA الرسمي القابل للقراءة الآليًا الذي توفره NIST (Fundamental Physical Constants — ASCII/JSON listing)، وليس الاستنتاج من صفحات HTML قابلة للتغيير.

كل عملية سحب بيانات يجب أن:

1. تُخزّن **snapshot** كامل للملف المصدري وقت السحب (وليس رابطًا فقط)، لأن BIPM/NIST تُحدّث صفحاتها بلا إشعار، والرابط وحده لا يثبت ما كانت عليه القيمة وقت التحقق.
2. تُسجّل hash (SHA-256) للملف المؤرشَف.
3. تُسجّل `retrieved_at` بصيغة ISO 8601.
4. لا تستبدل مصدرًا موثوقًا تلقائيًا بآخر أقل موثوقية عند فشل السحب (انظر القسم 26).

---

## 6. البيانات العددية

أي قيمة عددية علمية يجب أن تكون: موثقة، مرتبطة بمصدر، مرتبطة بإصدار أو تاريخ عند الحاجة، محفوظة بدقة مناسبة، مصحوبة بالوحدة، ومصحوبة بعدم اليقين عندما ينطبق ذلك.

يجب التمييز دائمًا بين: Exact value · Measured value · Derived value · Conventional value.

### 6-ب. قاعدة التخزين العددي ✏️ 🆕

**لا يجوز تخزين أي قيمة ثابت فيزيائي كنوع `float`/`double` في JSON أو في أي قاعدة بيانات.**

السبب: القيم مثل $G = 6.67430(15)\times10^{-11}$ لها عدد أرقام معنوية محدد وحرج علميًا، والتمثيل العائم (floating point) يعرّضها لفقدان دقة صامت عبر عمليات القراءة/الكتابة المتكررة.

القاعدة:

* تُخزَّن القيمة والـ uncertainty كـ **نص (string)** يحافظ على الأرقام المعنوية الأصلية كما وردت في المصدر.
* التحويل إلى نوع عددي (`decimal.Decimal` في Python) يتم فقط عند الحساب، وليس عند التخزين.
* الأس العشري (exponent) والوحدة يُخزّنان كحقول منفصلة، لا كجزء من نص حر.

---

## 7. المترولوجيا

لا تستخدم مصطلحات المترولوجيا بصورة عامة أو غير دقيقة. يجب التمييز بين: القيمة المحددة بالتعريف، القيمة المقاسة، القيمة المشتقة، القيمة المرجعية، uncertainty، relative uncertainty، conventional value.

عند التعامل مع ثوابت SI، يجب الرجوع إلى المصادر الرسمية ذات الصلة.

### 7-ب. التمييز الإلزامي بعد إعادة تعريف SI 2019 🆕

منذ 20 مايو 2019، أصبحت الثوابت التالية **قيمًا محددة تمامًا بالتعريف (defined exact)** ولا تحمل uncertainty:

$$
h,\quad e,\quad k_B,\quad N_A,\quad c
$$

بينما ثوابت أخرى مثل $G$، $m_e$، $\alpha$ (كقيمة تجريبية مستقلة) تبقى **مقيسة (measured)** وتحمل uncertainty قابلة للتحديث بين إصدارات CODATA.

هذا التمييز يجب أن يكون **حقل بيانات صريح** (`definitional_status: defined_exact | measured`) في كل سجل ثابت، وليس نصًا حرًا داخل المقال — لأن منطق التحقق (validation) والعرض في الواجهة يعتمد عليه مباشرة.

---

## 8. الوحدات

استخدم النظام الدولي للوحدات SI كمرجع أساسي. تحقق من: الوحدة، الأبعاد، التحويلات، الأسس، الاتساق البعدي.

لا تغير وحدة قيمة علمية دون توضيح التحويل.

---

## 9. الأبعاد والتحقق الفيزيائي

عند التعامل مع معادلة أو قيمة مشتقة، تحقق من:

$$
[\text{LHS}]=[\text{RHS}]
$$

يجب إجراء Dimensional Analysis عندما يكون ذلك مفيدًا للتحقق من صحة العلاقة. هذا التحقق يجب أن يكون **آليًا** عبر `dimensional_formula` مخزّنة لكل ثابت (انظر القسم 16-ب) وليس مراجعة يدوية فقط.

### 9-ب. التحقق التناسقي بين الثوابت (Cross-Consistency Check) 🆕

الثوابت الأساسية مترابطة رياضيًا؛ نظام CODATA نفسه مبني على *least-squares adjustment* لضمان اتساق آلاف القياسات مع بعضها. مثال:

$$
\alpha = \frac{e^{2}}{4\pi\varepsilon_{0}\hbar c}
$$

**قاعدة إلزامية:** يجب أن يتضمن المشروع سكربتًا عامًا (generic، متوافق مع القسم 24) يقوم بـ:

1. حساب الثوابت المشتقة من الثوابت الأساسية المخزّنة في القاعدة.
2. مقارنة الناتج بالقيمة المخزّنة ضمن هامش $n\sigma$ (عدد مرات عدم اليقين المُجمّع).
3. عند وجود تعارض خارج الهامش المسموح: تتحول حالة السجلّين المتعارضين تلقائيًا إلى `NEEDS_REVIEW`، **ولا يُسمح بالنشر** (انظر القسم 22).

هذا التحقق أهم بند علمي كان مفقودًا، لأنه الفرق بين "تخزين أرقام" و"قاعدة معرفة مترولوجية متسقة".

---

## 10. التاريخ العلمي

يجب التفريق بين: اكتشاف الظاهرة، تطوير النظرية، أول قياس، أول تقدير كمي، تطوير طريقة القياس، القيمة المرجعية الحديثة، التعريف المترولوجي.

لا تنسب اكتشافًا أو ثابتًا إلى عالم معين دون دليل موثوق.

---

## 11. التجارب

لا يتم اعتبار جميع التجارب المرتبطة بكمية فيزيائية قياسات مباشرة للثابت. استخدم تصنيفات: `direct_measurement` · `indirect_measurement` · `verification` · `educational_estimation`.

يجب التمييز بين تجربة تعليمية لتقدير كمية وتجربة مترولوجية دقيقة لتحديد ثابت فيزيائي.

### 11-ب. نموذج العلاقة تجربة↔ثابت 🆕

لا تُضمَّن التجارب كنص داخل ملف الثابت. تُبنى كعلاقة **many-to-many** مستقلة:

```text
experiment_id → {
  relates_to: [constant_id, constant_id, ...],
  relation_type: direct_measurement | indirect_measurement
               | verification | educational_estimation
}
```

السبب: تجربة واحدة (مثل ميزان الالتواء / كافانديش) قد تُستخدم تعليميًا لتقدير أكثر من كمية فيزيائية، وربطها بثابت واحد فقط يفقد هذه العلاقة ويكسر مبدأ القسم 15 (عدم تكرار البيانات) عند إعادة استخدامها في سياق آخر.

---

## 12. التجارب المخبرية في المقالات

عند ذكر التجارب المخبرية المرتبطة بالثابت داخل المقال: **اذكر أسماء التجارب فقط**. لا تحول مقال الثابت إلى دليل مختبري. تفاصيل التجربة تكون في صفحة التجربة المتخصصة.

---

## 13. قاعدة عدم اختلاق المحتوى

ممنوع اختلاق: قيمة علمية · uncertainty · تجربة · عالم · تاريخ · ورقة علمية · DOI · رابط · نتيجة تجريبية · صفحة من صفحات الموقع · بيانات تجريبية.

إذا لم يتم التحقق من المعلومة، يجب الإشارة إلى ذلك.

---

## 14. البيانات المنظمة قبل المقال

عند وجود معلومات يمكن تمثيلها كبيانات، يجب تخزينها في Structured Data قبل أو بالتوازي مع إنشاء المقال. المقال ليس قاعدة البيانات.

```text
Constant Record → Article → SEO → Publication
```

---

## 15. عدم تكرار البيانات

قبل إنشاء ملف جديد: تحقق من وجود الثابت، التجربة، المصدر، والمقال. إذا كان العنصر موجودًا: **حدّثه بدل إنشاء نسخة مكررة**.

### 15-ب. مسرد المصطلحات الموحّد (Glossary) 🆕

بما أن المحتوى عربي والمصادر إنجليزية، يجب أن يوجد ملف واحد `glossary.json` يثبّت الترجمة المعتمدة لكل مصطلح مترولوجي (مثال: "uncertainty" ← "عدم اليقين"، وليس "عدم التأكد" أو "الشك"؛ "reference value" ← "القيمة المرجعية"، وليس "القيمة المعيارية"). كل مقال وكل سكربت توليد محتوى يجب أن يستورد من هذا الملف ولا يخترع ترجمة بديلة. هذا امتداد لمبدأ "عدم التكرار" (القسم 15) مطبّقًا على المصطلحات لا فقط على الملفات.

---

## 16. التعديلات على Schema

لا تضف حقولًا عشوائية إلى JSON. إذا كان هناك احتياج حقيقي إلى معلومة جديدة: حدّد الحاجة → عدّل Schema → حدّث البيانات المتأثرة → تحقق من صحة الملفات → وثّق التغيير.

### 16-ب. نموذج Schema الأساسي (نواة إلزامية) 🆕

كل سجل ثابت (`/constants/{symbol}.json`) يجب أن يحتوي — كحد أدنى — على الحقول التالية:

| الحقل | النوع | ملاحظة |
|---|---|---|
| `symbol` | string | مثل `h`, `G`, `alpha` |
| `name_ar` / `name_en` | string | |
| `value` | **string** | وليس float — انظر القسم 6-ب |
| `unit` | string | SI فقط |
| `dimensional_formula` | string | للتحقق البعدي الآلي (القسم 9) |
| `definitional_status` | enum | `defined_exact` \| `measured` (القسم 7-ب) |
| `uncertainty_type` | enum | `exact` \| `standard` \| `expanded` |
| `standard_uncertainty` | string \| null | |
| `relative_uncertainty` | string \| null | |
| `coverage_factor_k` | number \| null | إن كانت expanded |
| `codata_version` | string | مثل `"CODATA 2022"` |
| `source_ref` | object | يشير لسجل في `/sources/` مع `retrieved_at` وhash |
| `verification_status` | enum | كما في القسم 31 |
| `history` | array | سلسلة القيم عبر إصدارات CODATA — انظر القسم 18-ب |

يجب أن تكون بنية البيانات مستقرة وقابلة للتوسع، ولا تُضاف حقول خارج هذه القائمة دون المرور بخطوات هذا القسم.

---

## 17. Version Control

كل تغيير مهم يجب أن يكون قابلًا للتتبع من خلال Git. يجب أن تكون الـcommits واضحة، قصيرة، ومرتبطة بالتغيير الفعلي.

أمثلة: `add: Planck constant record` · `update: CODATA reference data` · `fix: gravitational constant uncertainty` · `docs: improve constant article` · `ci: add weekly validation workflow`

---

## 18. عدم حذف التاريخ

عند تحديث قيمة ثابت: لا تحذف القيمة السابقة إذا كانت لها أهمية تاريخية أو مرجعية.

```text
Historical Value → Previous Reference → Current Reference
```

### 18-ب. تتبّع إصدارات CODATA كسلسلة زمنية 🆕

حقل `history` في كل سجل ثابت (القسم 16-ب) يُخزَّن كمصفوفة صريحة:

```json
"history": [
  {"codata_version": "CODATA 2014", "value": "...", "uncertainty": "...", "source_ref": "..."},
  {"codata_version": "CODATA 2018", "value": "...", "uncertainty": "...", "source_ref": "..."},
  {"codata_version": "CODATA 2022", "value": "...", "uncertainty": "...", "source_ref": "..."}
]
```

هذا ليس فقط أرشفة: تقلّص عدم اليقين النسبي عبر الزمن لثابت مثل $h$ أو $N_A$ محتوى تعليمي قوي بحد ذاته (رسم بياني واحد، منطق عام يعمل على أي ثابت له أكثر من إصدار — متوافق مع القسم 24).

---

## 19. التحقق قبل الدمج

أي بيانات أو كود جديد يجب أن يمر، حسب طبيعته، عبر: JSON validation · Schema validation · Scientific QA · Link validation · Code testing · Markdown validation · SEO validation عند الحاجة.

**لا تعتبر نجاح تشغيل الكود دليلًا على صحة المحتوى العلمي.**

### 19-ب. خطوات CI محددة (تفعيل عملي للقسم أعلاه) 🆕

1. `jsonschema` validation لكل ملف متأثر بالـ PR.
2. سكربت عام للتحقق البعدي مقابل `dimensional_formula` (القسم 9).
3. سكربت التحقق التناسقي بين الثوابت (القسم 9-ب) — **بند إلزامي، ليس اختياريًا**.
4. فحص الروابط (link check) على الروابط المؤرشفة فقط دون توليد روابط جديدة (القسم 27).
5. لا يُسمح بالدمج (merge) عند فشل أي بند علمي أعلاه، بصرف النظر عن نجاح باقي الكود.

---

## 20. GitHub Actions

يجب أن تكون عمليات الأتمتة قابلة لإعادة التشغيل، قابلة للتتبع، آمنة، وidempotent قدر الإمكان.

يجب ألا يؤدي تشغيل Workflow مرتين إلى: إنشاء المقال مرتين، تكرار البيانات، تكرار المصادر، أو إنشاء commits غير ضرورية.

---

## 21. الأسرار وبيانات الدخول

يمنع منعًا باتًا وضع API Keys · Tokens · Passwords · WordPress credentials · Service account credentials داخل المستودع.

استخدم `GitHub Secrets` أو بيئة أسرار مناسبة. لا تسجل الأسرار في logs.

---

## 22. الأتمتة الأسبوعية

الجدولة الأسبوعية ليست هدفًا بحد ذاته. إذا فشل التحقق العلمي (بما فيه التحقق التناسقي في القسم 9-ب):

```text
DO NOT PUBLISH
```

يجب أن تكون جودة البيانات أهم من الالتزام بموعد النشر.

---

## 23. Human Review

في المرحلة الأولى:

```text
Research → Generate → QA → Human Review → Publish
```

لا يتم الانتقال إلى النشر التلقائي الكامل إلا بعد إثبات استقرار النظام.

---

## 24. قابلية التوسع

لا تكتب حلولًا مخصصة لثابت واحد. أي كود يجب أن يعمل على $G, h, c, e, k_B, N_A, \alpha, \dots$ دون الحاجة إلى تعديل الكود لكل ثابت.

تجنب: hard-coded values · أسماء ملفات ثابتة · مسارات خاصة بثابت واحد · منطق يعتمد على ترتيب معين · معالجة مبنية على حالة واحدة.

---

## 25. تصميم الكود

استخدم Python · JSON · JSON Schema · Environment Variables · Modular Functions · Logging · Exception Handling · Type Hints عند الحاجة · Unit Tests عندما يكون ذلك مناسبًا.

افصل بين: Research · Validation · Data Processing · Article Generation · SEO · Publishing. ولا تضع جميع العمليات في ملف واحد ضخم.

---

## 26. التعامل مع أخطاء المصادر

إذا كان المصدر غير متاح، متناقضًا، قديمًا، غير واضح، أو لا يحتوي البيانات المطلوبة: **لا تستبدله تلقائيًا بمصدر أقل موثوقية دون تسجيل ذلك**.

يجب تسجيل `source_status` · `verification_status` · `last_verified` عند الحاجة.

---

## 27. الروابط

لا تنشئ رابطًا إلا بعد التحقق من وجوده. لا تخترع URL.

إذا تغير رابط مصدر موثوق: حدّث الرابط، احتفظ بسجل مناسب للتغيير، تحقق من الروابط المتأثرة.

---

## 28. SEO

SEO جزء من المشروع لكنه **ليس معيارًا لتحديد الحقيقة العلمية**.

لا يجوز: تغيير معلومة صحيحة من أجل Keyword · حذف uncertainty لتحسين قابلية القراءة · تبسيط مفهوم علمي بطريقة تجعله خاطئًا · تكرار الكلمات المفتاحية بشكل مصطنع.

**Scientific Accuracy > SEO**

---

## 29. أسلوب المحتوى

المحتوى العلمي يجب أن يكون عربيًا واضحًا، دقيقًا، أكاديميًا، منظمًا، مختصرًا عندما لا تحتاج المعلومة إلى توسع، ومدعومًا بالمصادر.

تجنب: المبالغة · اللغة التسويقية · الادعاءات المطلقة · الحشو · تكرار المعلومة.

---

## 30. قابلية التتبع Provenance

كل معلومة علمية مهمة يجب أن يمكن تتبعها إلى مصدرها، خصوصًا: القيمة · uncertainty · التعريف · الوضع المترولوجي · القياسات · التجارب · التواريخ المهمة.

```text
Claim → Source → Reference Version → Verification Date
```

---

## 31. حالات البيانات

استخدم حالات واضحة: `RESEARCHING` · `VALIDATING` · `VERIFIED` · `NEEDS_REVIEW` · `OUTDATED` · `UPDATE_REQUIRED`.

ولا تعتبر البيانات `VERIFIED` إلا بعد التحقق المطلوب (بما فيه التحقق التناسقي، القسم 9-ب).

---

## 32. التحديثات المستقبلية

صمم النظام بحيث يستطيع التعامل مع: إصدارات CODATA الجديدة · تحديثات SI · تغير المراجع · تصحيح الأخطاء · إضافة ثوابت جديدة · إضافة تجارب جديدة · إضافة روابط جديدة.

يجب ألا يتطلب تحديث ثابت واحد إعادة بناء النظام كاملًا.

---

## 33. مبدأ عدم الإفراط في التعقيد

ابدأ بأبسط بنية تحقق الهدف. لا تضف قواعد بيانات معقدة، خدمات خارجية، APIs غير ضرورية، أو طبقات برمجية غير مطلوبة إلا عندما توجد حاجة فعلية. لكن حافظ على قابلية التوسع مستقبلًا.

---

## 34. الأولوية عند التعارض

1. الصحة العلمية.
2. سلامة البيانات.
3. المصادر والتوثيق.
4. سلامة الكود.
5. قابلية الصيانة.
6. قابلية التوسع.
7. جودة المحتوى.
8. SEO.
9. سرعة التنفيذ.

---

## 35. قاعدة العمل النهائية

قبل تنفيذ أي مهمة، اسأل:

1. هل توجد بيانات سابقة يمكن إعادة استخدامها؟
2. هل توجد Schema يجب الالتزام بها؟
3. ما المصدر العلمي الأساسي؟
4. هل المعلومة Verified؟
5. هل هي Exact أم Measured أم Derived؟ **وهل `definitional_status` مضبوط بشكل صريح؟** 🆕
6. هل توجد تجربة مرتبطة، وهل نموذج العلاقة many-to-many محفوظ بشكل صحيح؟ 🆕
7. هل العلاقة Direct أم Indirect أم Educational؟
8. هل التغيير سيؤثر على ملفات أخرى، بما في ذلك التحقق التناسقي مع ثوابت أخرى؟ 🆕
9. هل يمكن تنفيذ الحل بشكل عام بدل تخصيصه لحالة واحدة؟
10. هل يمكن التحقق من النتيجة آليًا؟

---

## هيكل المستودع المقترح 🆕

```text
/constants/{symbol}.json          ← البيانات المنظمة (مصدر الحقيقة)
/experiments/{id}.json
/sources/{id}.json                ← بيانات المصدر + snapshot + hash
/schemas/*.schema.json
/glossary.json                    ← مسرد المصطلحات الموحّد (القسم 15-ب)
/scripts/
    ingest_codata.py              ← الابتلاع الآلي (القسم 5-ب)
    validate_schema.py
    validate_dimensions.py        ← القسم 9
    validate_consistency.py       ← التحقق التناسقي — القسم 9-ب (الأهم)
    generate_article.py
/articles/{symbol}.md
/.github/workflows/
    validate.yml
    weekly_pipeline.yml
```

---

## المبدأ النهائي

هذا المشروع ليس **AI Article Generator**، بل:

**Scientific Knowledge Base + Automated Research Pipeline + Content Publishing System**

ويجب أن تبقى القاعدة الأساسية:

> **لا تنشر ما لا تستطيع التحقق منه، ولا تخزن معلومة علمية مهمة دون مصدر، ولا تجعل المقال مصدر الحقيقة عندما يمكن تمثيل المعلومة كبيانات منظمة — ولا تعتبر البيانات متسقة حتى تُختبر رياضيًا مقابل بقية الثوابت.** 🆕
